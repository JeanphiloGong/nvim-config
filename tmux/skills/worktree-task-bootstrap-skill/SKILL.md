---
name: worktree-task-bootstrap-skill
description: v0.1.0 - Deprecated compatibility alias for task-worktree-bootstrap-skill.
---

# Deprecated Alias

Use `task-worktree-bootstrap-skill` instead.

This legacy name exists only so older prompts and task templates do not fail
hard during the naming transition.

Canonical replacement:

- `task-worktree-bootstrap-skill`

Role note:

- the canonical bootstrap skill is now an internal helper beneath
  `project-window-orchestrator-skill`, not the main public entry
